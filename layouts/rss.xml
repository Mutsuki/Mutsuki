{{- /* 初始化变量 */}}
{{- $authorEmail := "" }}
{{- $authorName := "" }}

{{- /* 尝试从 site.Params.author 中获取 email 和 name */}}
{{- with site.Params.author }}
    {{- if reflect.IsMap . }}
        {{- with .email }}
            {{- $authorEmail = . }}
        {{- end }}
        {{- with .name }}
            {{- $authorName = . }}
        {{- end }}
    {{- end }}
{{- else }}
    {{- /* 使用 site.Author 作为备用 */}}
    {{- with site.Author.email }}
        {{- $authorEmail = . }}
        {{- warnf "The author key in site configuration is deprecated. Use params.author.email instead." }}
    {{- end }}
    {{- with site.Author.name }}
        {{- $authorName = . }}
        {{- warnf "The author key in site configuration is deprecated. Use params.author.name instead." }}
    {{- end }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome -}}
    {{- $pctx = .Site }}
{{- end }}

{{- $pages := $pctx.RegularPages }}
{{- if not (or $.IsHome $.IsSection) -}}
    {{- $pages = $pctx.Pages }}
{{- end }}

{{- $limit := .Site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 -}}
    {{- $pages = $pages | first $limit }}
{{- end }}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="https://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ .Title }} on {{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}in {{ .Title }} {{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{ . }}</language>{{ end }}
    {{- if $authorEmail }}
    <managingEditor>{{ $authorEmail }}{{ if $authorName }} ({{ $authorName }}){{ end }}</managingEditor>
    <webMaster>{{ $authorEmail }}{{ if $authorName }} ({{ $authorName }}){{ end }}</webMaster>
    {{- end }}
    {{- with .Site.Copyright }}
    <copyright>{{ . }}</copyright>{{ end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ if $authorEmail }}
      <author>{{ $authorEmail }}{{ if $authorName }} ({{ $authorName }}){{ end }}</author>
      {{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>
        {{ with .Params.featured_image }}
        {{ printf "<![CDATA[<img src=\"%s\" width=\"800\" height=\"150\" />]]>" (. | absURL) | safeHTML }}
        {{ end }}
        {{ .Summary | html }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
